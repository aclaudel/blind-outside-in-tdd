import React from "react";
import {Spinner} from "react-bootstrap";

export default function LoadingSection(props) {
  throw new Error('Loading Section component not yet implemented')
  return <div />
}